<!--
  The privacy policy of «Дневник». Product text, not a project document: the app bundles
  this file as an asset and opens it from the line under the first screen's button and from
  «Настройки → О приложении», so it is written in Russian first, like values/.

  privacy.ru.md is the source and privacy.en.md the translation. Both keep the same sections,
  in the same order, with the same ids, built from the same blocks — the same paragraphs, the
  same number of points in each list, the same numbered steps.

  The format is the Markdown subset the app's guide is written in (DocsMarkdown.kt in
  :core:data): `#` is the title, `##` a section with its id, label and summary on the one-line
  comment directly under it; then paragraphs, `-` lists, numbered steps with a bold lead and
  `>` notes, each written on one line; inside a line only bold, code spans and links, and a
  link must be absolute https because the in-app copy has no folder to be relative to. No
  tables and no images. A comment inside a section must span several lines, as the ones
  below do: a one-line comment is read as the section's metadata and replaces its id.

  This text describes the code, and nothing else holds it true. A change to what is stored,
  sent or kept, or for how long, changes this file in the same batch and raises the edition
  in legal.json. Before adding a promise, check it against what this code does not do: the
  password does cross the server on the bot's sign-in page and from older apps; the data is
  not in Russia; the operator holds the key and can read the diary; leaving a class deletes
  nothing on the server; the phone encrypts nothing itself; an http:// server address
  carries everything in the clear; the keep-alive runs for up to 30 days after the last
  use, not only while somebody is active; and no diary has yet been opened for real.

  Passages under a comment that starts with FORK describe the upstream deployment. A fork
  that runs its own server rewrites them; the bracketed names in each comment are the fields
  to fill. Every github.com/lumenpearson/lessons link is the field [REPOSITORY].
-->

# Политика конфиденциальности «Дневника»

## Кто отвечает за данные
<!-- id: OPERATOR; label: Оператор; summary: Кто хранит данные и о каком сервере этот текст -->

Этот текст описывает, что делают с данными приложение «Дневник», его сервер и Telegram-бот. Сказанное о приложении верно для любой его сборки из этого кода, сказанное о сервере — для сервера, запущенного из этого кода без изменений, а оператор и место хранения названы ниже только для одного сервера — того, который запускает этот оператор.

<!--
  FORK: [OPERATOR] [HOSTING] [DB] [SERVER_ADDRESS] [BOT]
  The list below is the upstream deployment: the operator is the maintainer of
  github.com/lumenpearson/lessons; the hosting is Vercel, region fra1 (vercel.json); the
  database is Neon, region aws-eu-central-1 (read from the Neon project, not from the
  repository). A fork that runs its own server rewrites the first two points. The upstream
  names no server address and no bot on purpose: the repository publishes neither, and the
  APK has no server built in. A fork with a public address or bot may add them to the list,
  keeping the Russian and English lists the same length.
-->

- **Оператор** — сопровождающий репозитория [lumenpearson/lessons](https://github.com/lumenpearson/lessons) на GitHub.
- **Где работает сервер** — хостинг Vercel, регион `fra1` (Франкфурт-на-Майне, Германия); база данных — Neon, регион `aws-eu-central-1` (тоже Франкфурт).
- **Как связаться** — в разделе «Контакты» в конце.

Адрес сервера вы вводите в приложении сами. Если он ведёт на сервер, который запустил кто-то другой, за данные на том сервере и в его боте отвечает тот, кто его запустил, и описывать их должна его политика.

## Что хранится на телефоне
<!-- id: PHONE; label: Телефон; summary: Что приложение держит у себя и что не попадает в резервную копию -->

- Для каждого класса, к которому вы подключились, — ключ доступа, название класса и школы.
- Копия расписания класса, событий и заданий, включая имена учителей, — чтобы приложение и виджет работали без сети.
- Если вы вошли в дневник, — выданный сервером ключ доступа к дневнику, ваш логин, какой это дневник, регион и школа и какого ученика вы выбрали; а в отдельной базе — копия дневника: уроки, задания, оценки, имена учеников и учителей.
- Адрес сервера, настройки приложения и, если вы входили в GitHub из приложения, ключ GitHub и имя вашей учётной записи там.
- Отчёты о сбоях — только если вы их включили: до пяти файлов с моделью телефона, версиями Android и приложения, описанием ошибки и последними записями о работе приложения.

Ключи, логин и настройки лежат в файлах настроек приложения **без шифрования**. Базы данных, файлы настроек и отчёты о сбоях исключены из резервной копии Google и из переноса на новый телефон: отчёт остаётся на этом телефоне, пока вы сами его не отправите. Что отчёты о сбоях действительно не попадают в копию, на устройстве пока не проверялось.

Пароль от дневника телефон не сохраняет нигде. Сессию, которую дневник выдаёт после входа, телефон держит только в памяти, не записывая, и забывает, как только передал её серверу или вход отменён.

## Что получает и хранит сервер
<!-- id: SERVER; label: Сервер; summary: Телефоны, сессии дневника, правки, поиск школы, бот -->

Когда телефон подключается к классу, сервер сохраняет:

- ключ доступа телефона — только в виде хеша SHA-256, по которому ключ не восстановить;
- название телефона — производитель и модель;
- когда телефон последний раз обращался к серверу;
- учётную запись Telegram, если телефон к ней привязали.

Когда вы входите в дневник, сервер сохраняет:

- сессию дневника — **зашифрованной** ключом оператора; у «Сетевого города» вместе с ней внутри шифра лежат регион, номер и название школы и сведения об учебном годе;
- ваш логин — так, как вы его ввели, открытым текстом; в «Петербургском образовании» это адрес электронной почты;
- какой это дневник и регион, когда сессию открыли, когда ею последний раз пользовались, когда сервер её поддерживал и когда дневник перестал её принимать;
- ключ доступа к дневнику, выданный телефону, — только в виде хеша SHA-256.

Сессия, открытая в приложении, не связана ни с учётной записью Telegram, ни с классом, и бот её не видит. Сессия, открытая из бота, связана с вашей учётной записью Telegram и с классом, и в ней запоминается выбранный ученик.

Ваши правки дневника: что вы поправили, новое значение и то, что было в дневнике в момент правки. Они привязаны к вашему логину и номеру ученика, а не к сессии, поэтому остаются после выхода из дневника.

Если оператор включил поиск школ, при выборе дневника можно найти свой регион по названию школы: сервер передаёт набранный текст сервису DaData. В базу сервер этот текст не записывает, но он входит в адрес запроса и поэтому остаётся в журнале запросов хостинга.

Чтобы ограничить подбор кодов и паролей, сервер считает неудачные попытки подключиться к классу и войти в дневник, а также каждый поиск школы. Для этого он записывает хеш SHA-256 вашего IP-адреса без секретного ключа. Такой хеш можно обратить перебором всех адресов, поэтому это не обезличивание: это персональные данные.

Если вы пользуетесь ботом, сервер хранит:

- номер вашей учётной записи Telegram, имя пользователя и имя, как они указаны в Telegram, и вашу роль в классе;
- номер телефона — только если вы сами поделились контактом и для него нашлось приглашение; номер без приглашения не сохраняется;
- приглашения по номеру телефона, которые оформили администраторы, с подписями к ним;
- ваши задачи, отметки «сделал», время напоминаний и запросы прав вместе с их текстом;
- журнал изменений класса — кто и что поменял; в его строках бывают имена и номера телефонов;
- незаконченный ввод в боте и класс, который вы выбрали, если их у вас несколько.

Данные самого класса — расписание, замены, задания, события, имена учителей и то, кто добавил задание, — хранятся на сервере, пока существует класс.

## Чего сервер не хранит
<!-- id: NEVER; label: Не хранит; summary: Пароль, содержимое дневника, аналитика -->

**Пароль от дневника сервер не хранит.** Приложение, в котором есть этот текст, отправляет пароль только в систему дневника, прямо с телефона, и сервер его не получает.

Через сервер пароль всё же проходит в двух случаях: когда вы входите в дневник на странице, которую открывает бот, — браузер отправляет пароль серверу, а сервер один раз передаёт его дневнику, — и когда входят старые версии приложения, в которых этого текста ещё нет. В обоих случаях сервер нигде пароль не записывает.

**Содержимое дневника** — оценки, задания, расписание, список учеников — сервер читает по вашему запросу и передаёт дальше, не сохраняя. Исключение — то, что было в записи, которую вы поправили.

**Аналитики, рекламных идентификаторов и автоматической отправки отчётов о сбоях** нет ни в приложении, ни на сервере.

## Как устроен вход в дневник
<!-- id: SIGN_IN; label: Вход; summary: Куда уходит пароль и что получает сервер -->

1. **Форма** — Вы вводите логин и пароль в форме приложения; в ней написано, в какой дневник и на какой адрес уйдёт пароль.
2. **Дневник** — Приложение отправляет их по HTTPS прямо на сервер системы дневника — только на адреса из списка, встроенного в приложение, — и получает в ответ сессию.
3. **Сервер** — Приложение передаёт серверу эту сессию и ваш логин, а для «Сетевого города» — ещё регион и школу. Сервер проверяет сессию своим обращением к дневнику (к «Сетевому городу» — четырьмя), шифрует её и выдаёт приложению свой ключ доступа к дневнику.
4. **Дальше** — Телефон забывает сессию дневника и читает дневник через сервер, а сервер обращается к дневнику со своего адреса.

Если дневник не принял сессию с адреса сервера, сервер её не сохраняет и отвечает приложению, что дело не в пароле, а в том, откуда пришла сессия.

В Госуслуги и в «Мою школу» приложение не входит: оно открывает страницу системы в вашем браузере, и ничего из Госуслуг не попадает ни в приложение, ни на сервер.

## Кто может прочитать ваш дневник
<!-- id: READERS; label: Кто читает; summary: Система дневника, оператор, Telegram и тот, у кого телефон -->

- **Система дневника** после входа видит, что вашей сессией пользуется адрес сервера, а не вашего телефона.
- **Оператор** держит ключ, которым зашифрованы сессии, и поэтому технически может открыть ваш дневник. Шифрование защищает от того, к кому попадёт база данных без ключа, а не от оператора.
- **Telegram** хранит в истории чата то, что бот прислал вам из дневника, — оценки, задания, расписание.
- **Тот, у кого в руках ваш телефон:** ключ доступа к дневнику лежит на нём без шифрования, и приложение открывает дневник без пароля, пока вы из него не вышли.

## Поддержание сеанса
<!-- id: KEEPALIVE; label: Сеанс; summary: Как сервер держит сессию «Сетевого города» открытой и как долго -->

Сессия «Сетевого города» сама закрывается после четверти часа или часа без обращений, а пароля, чтобы войти снова, у сервера нет. Поэтому, пока идут часы сервера — внешний сервис, который вызывает сервер каждые несколько минут, если оператор его настроил, — сервер обращается к «Сетевому городу» с каждой открытой сессией не чаще раза в четыре минуты, чтобы она не закрылась.

Так продолжается до 30 дней с тех пор, как вы в последний раз входили в дневник или читали его через сервер — в приложении или в боте; потом сервер удаляет сессию. Сами обращения для поддержания сеанса пользованием не считаются и этот срок не продлевают.

Пользованием считается чтение дневника приложением, открытым на экране, или ботом. Виджет и фоновая синхронизация дневник не читают и сессию не продлевают.

Сессии «Петербургского образования» сервер не поддерживает: они живут столько, сколько позволяет сама система, и этот срок нигде не описан.

## Сколько хранится
<!-- id: RETENTION; label: Сроки; summary: Что удаляется само, когда, и что не удаляется -->

- Хеш IP-адреса от неудачной попытки или поиска школы — не дольше часа.
- Сессия дневника — 30 дней после последнего пользования; сессия, которую дневник перестал принимать, — сутки после того, как это выяснилось.
- Запись о телефоне в классе — 180 дней после последнего обращения; ни выход из класса в приложении, ни отключение телефона администратором не удаляют её раньше.
- Ссылка для входа в дневник из бота — 15 минут; личный код для подключения телефона — сутки после того, как истекли его 15 минут.
- Незаконченный ввод в боте — 2 дня без изменений.

**Без срока** — пока их не удалят или пока не удалят класс — хранятся журнал изменений, задачи и отметки «сделал», время напоминаний, запросы прав, приглашения по номеру телефона, ваше участие в классе в боте и данные класса. Правки дневника и класс, выбранный в боте, не удаляются и вместе с классом; правки удаляет кнопка «Сбросить правки».

Эти сроки выдерживают часы сервера. Если оператор их не настроил, записи из этого списка остаются и после срока, а сессии «Сетевого города» не поддерживаются.

<!--
  FORK: [DB_HISTORY] [HOSTING_LOGS]
  The paragraph below is the upstream's hosting: the Neon project keeps 21600 seconds of
  history (history_retention_seconds), and Vercel keeps request logs for as long as its plan
  does — a number the repository does not hold. A fork on other hosting describes its own,
  including whether it logs request URLs.
-->

База данных этого сервера (Neon) хранит историю изменений ещё 6 часов, так что удалённое можно восстановить в течение этого времени. Хостинг (Vercel) ведёт журналы запросов с IP-адресом, адресом запроса и временем; в некоторых адресах есть секрет — например, ссылка на календарь класса или ссылка для входа в дневник из бота. Сколько хостинг хранит эти журналы, определяют его правила.

## Третьи стороны
<!-- id: THIRD_PARTIES; label: Третьи стороны; summary: Кому и что уходит помимо сервера -->

<!--
  FORK: [HOSTING] [DB] [DADATA] [REPOSITORY]
  «Vercel и Neon» are the upstream's. DaData applies only where DADATA_TOKEN is set; without
  it the search refuses at the door and Settings.disabled_features logs it as off. The GitHub
  point holds for every build of this code: the update check and the guide are fetched from
  lumenpearson/lessons, hard-coded in GithubApi.kt, whatever repository built the APK.
-->

- **Хостинг и база данных** — у этого оператора Vercel и Neon: на их серверах работает сервер и лежит всё, что он хранит.
- **Telegram** — через него проходит всё, что вы делаете в боте и что бот вам присылает.
- **Системы дневников** — «Петербургское образование» и региональные серверы «Сетевого города». Телефон отправляет им логин и пароль при входе, а «Сетевому городу» — и название школы, когда вы её ищете; сервер отправляет им ваши запросы к дневнику.
- **DaData** — если оператор включил поиск школ: сервер отправляет туда набранное название школы, но не ваш IP-адрес.
- **GitHub** — приложение раз в сутки при запуске проверяет там, нет ли новой версии (включено по умолчанию, выключается в настройках), и загружает оттуда руководство, когда вы его открываете; GitHub при этом видит адрес телефона и версию приложения. Отчёт об ошибке и исправление перевода попадают туда публично — только если вы сами их отправили.
- **Google** — резервная копия телефона, но лишь то, что из неё не исключено (см. «Что хранится на телефоне»).
- **Госуслуги и сайты систем дневников**, которые приложение открывает в вашем браузере: там действуют их собственные правила.

## Где хранятся данные
<!-- id: ABROAD; label: Где; summary: Страна, в которой лежат данные сервера -->

<!--
  FORK: [COUNTRY]
  Germany is where the upstream's Vercel region (fra1) and Neon region (aws-eu-central-1)
  are. A fork states its own country.
-->

Сервер этого оператора и его база данных находятся в Германии, во Франкфурте-на-Майне. Всё, что хранит сервер, хранится не в России, и данные с телефона в России уходят за её пределы.

Если вы подключились к другому серверу, где хранятся данные, решает тот, кто его запустил.

## Дети и родители
<!-- id: CHILDREN; label: Дети; summary: Согласие родителей и то, чего приложение не проверяет -->

Дневник — это сведения о ребёнке: его оценки, задания и расписание. Если «Дневником» пользуется ученик младше 18 лет, это должно происходить с ведома и согласия его родителя или другого законного представителя.

Приложение не спрашивает возраст и ничего не проверяет о том, кто входит. Родительская учётная запись дневника может показывать нескольких детей, и всех их видит каждый, кто в неё вошёл.

Родитель, который хочет, чтобы сервер удалил данные его ребёнка, может написать оператору — способ указан в разделе «Контакты».

## Защита
<!-- id: SECURITY; label: Защита; summary: Хеши, шифрование сессии и чем опасен адрес http:// -->

- Ключи доступа телефона и дневника и одноразовые коды — для входа из бота и для подключения телефона — сервер хранит только в виде хеша SHA-256. Код класса хранится как есть: бот показывает его администраторам.
- Сессию дневника сервер шифрует (Fernet: AES-128-CBC с HMAC) ключом, который задаёт оператор. Без ключа дневник на сервере не работает вовсе, а не хранит сессии открыто.
- Приложение само ничего не шифрует на телефоне: там данные защищены лишь тем, чем защищён сам телефон.

**HTTPS зависит от адреса, который вы ввели.** Приложение принимает и адреса `http://` — ради серверов в локальной сети школы. По такому адресу всё, что идёт между телефоном и сервером, передаётся открытым текстом: ключи доступа к классу и к дневнику, название телефона, расписание и сессия дневника, которую телефон передаёт серверу при входе. Любой в той же сети может её перехватить и открыть ваш дневник. Приложение предупреждает об этом перед входом в дневник, но не запрещает.

В приложении пароль от дневника при любом адресе сервера уходит только в систему дневника и только по HTTPS.

## Что вы можете сделать
<!-- id: CHOICES; label: Ваш выбор; summary: Выйти, сбросить, выключить — и что нельзя сделать самому -->

- Выйти из дневника в приложении или в боте — сервер удалит сессию. Если телефон в этот момент не в сети, сессия удалится сама через 30 дней после последнего пользования, а сессию «Сетевого города» сервер до тех пор продолжит поддерживать.
- Сбросить свои правки дневника.
- Выйти из класса — это сотрёт копию на телефоне, но не запись о телефоне на сервере.
- Выключить автоматическую проверку обновлений. Отчёты о сбоях выключены, пока вы сами их не включите.

Удалить себя из бота или свои данные с сервера самостоятельно нельзя: таких команд нет, для этого напишите оператору. Историю чата в Telegram и данные в системе дневника оператор удалить не может — это делается там.

## Изменения
<!-- id: CHANGES; label: Изменения; summary: Редакции, дата и что меняет этот текст -->

Номер редакции и дата, с которой она действует, записаны в файле `legal.json` рядом с этим текстом. В приложении есть копия той редакции, с которой его собрали; действующая открывается по ссылке, если сборка эту ссылку знает.

Если меняется то, что код хранит, передаёт или как долго держит, меняется и этот текст — с новым номером редакции. Продолжая пользоваться «Дневником» после этого, вы принимаете новую редакцию; нигде не записывается, когда вы её приняли.

Этот текст написан по-русски, английский — его перевод. Если они расходятся, действует русский.

## Контакты
<!-- id: CONTACT; label: Контакты; summary: Вопросы о данных и просьбы их удалить -->

<!--
  FORK: [OPERATOR] [CONTACT]
  Both paragraphs below are the upstream's: the operator is the maintainer of
  github.com/lumenpearson/lessons, and the contact is that repository's public Issues page
  plus the «Отправить письмом» address a build may carry (LESSONS_CONTACT_EMAIL). A fork
  names its own operator and a contact it actually reads.
-->

Оператор этого сервера — владелец учётной записи `lumenpearson` на GitHub, который сопровождает репозиторий [lumenpearson/lessons](https://github.com/lumenpearson/lessons).

Вопросы о ваших данных и просьбы их удалить пишите на [странице Issues репозитория](https://github.com/lumenpearson/lessons/issues). Она открыта всем, поэтому не пишите там логинов, паролей, телефонов и имён детей — только суть просьбы. Если в вашей сборке есть кнопка «Отправить письмом» («Настройки → О приложении → Сообщить об ошибке»), можно написать и письмом.
