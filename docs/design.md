# Дизайн-система

Внешний вид приложения и виджета целиком построен на
[Essentials](https://github.com/sameerasw/essentials) (MIT, © sameerasw.com).
Это не «вдохновлено»: шрифт, шкала типографики, форма контейнеров, компоненты
и почти все настройки персонализации перенесены из репозитория, а не придуманы
заново.

## Грамматика

Весь интерфейс — это три вещи:

* **страница** — `surfaceContainer`, поля 16 dp;
* **группа** — `RoundedCardContainer`: скругление 24 dp, прозрачный фон,
  промежутки между строками 2 dp. Группа — это только маска. Своей заливки у
  неё нет, углы скругляет она, а не строки;
* **строка** — прямоугольник на `surfaceBright` с круглой цветной плиткой
  слева.

Разделителей нет нигде. Два блока разделяются тем, что один из них становится
группой, а не линией между ними. Из виджета `ThinDivider` удалён по той же
причине.

## Что откуда взято

| Essentials | Здесь |
| --- | --- |
| `res/font/google_sans_flex.ttf` | тот же файл в `:core:designsystem` |
| `ui/theme/Type.kt` | `theme/Type.kt` — шкала дословно |
| `ui/theme/Shapes.kt` | `theme/Shape.kt` |
| `ui/core/containers/RoundedCardContainer.kt` | `component/Group.kt` |
| `ui/components/EssentialsFloatingToolbar.kt` | `component/FloatingToolbar.kt` |
| `ui/core/sheets/EssentialsBottomSheet.kt` | `component/BottomSheet.kt` |
| `ui/core/pickers/SegmentedPicker.kt` | `component/SegmentedPicker.kt` |
| `ui/components/sliders/ConfigSliderItem.kt` | `component/SliderItem.kt` |
| `ui/core/cards/IconToggleItem.kt` | `component/Group.kt` (`GroupSwitchItem`) |
| `ui/modifiers/ScrollMotionBlurModifier.kt` | `modifier/ScrollMotionBlur.kt` |
| `ui/modifiers/ProgressiveBlurModifier.kt` | `modifier/ProgressiveBlur.kt` |
| `ui/components/modifiers/ShimmerModifier.kt` | `modifier/Shimmer.kt` |
| `utils/ui/HapticUtil.kt` | `haptic/LessonsHaptics.kt` |
| `MainActivity` (пейджер вкладок) | `navigation/LessonsApp.kt` |
| `ui/activities/SettingsActivity.kt` | `ui/settings/SettingsScreen.kt` |
| `ui/composables/WelcomeScreen.kt` | `ui/onboarding/` — четыре шага первого запуска |

Шейдеры AGSL — оба, размытие при прокрутке и размытие под шторкой — перенесены
посимвольно. Их подбирали на глаз, и «улучшенная» константа в них ломает эффект.

## Что исправлено против оригинала

Три бага Essentials перенесены не были — вместо этого исправлены:

1. **Пустой слот FAB.** `EssentialsFloatingToolbar` всегда передаёт
   `floatingActionButton`, подставляя `{}`, когда кнопки нет.
   `HorizontalFloatingToolbar` всё равно раскладывает переданный слот, поэтому
   пустой резервирует ширину несуществующей кнопки и прижимает панель к левому
   краю с дырой сбоку. Здесь слот передаётся только когда в нём что-то есть.
2. **Промежутки в свёрнутой панели.** Отступ между кнопками анимируется к 8 dp
   для всех, кроме последней, — в том числе когда панель свёрнута и все
   невыбранные кнопки уже имеют нулевую ширину. Внутри свёрнутой «таблетки»
   остаётся `(n − 1) × 8 dp` пустоты. Здесь отступ схлопывается вместе с
   кнопками.
3. **Порог подписи.** Подпись выбранной вкладки скрывается при ширине экрана
   меньше 400 dp. Большинство телефонов в портретной ориентации — 360 dp, то
   есть раскрывающаяся подпись, ради которой компонент и написан, не
   появлялась ни на одном из них. Реальный минимум — 328 dp; здесь стоит 330.

4. **Шейдер компилируется каждый кадр.** `ProgressiveBlurModifier` создаёт
   `RuntimeShader` *внутри* блока `graphicsLayer`, то есть исходник AGSL
   разбирается и компилируется при каждой отрисовке слоя — а слой этот и там, и
   здесь занимает весь экран. Здесь шейдер создаётся один раз и запоминается.

Открытые pull request'ы Essentials просмотрены: исправлений для перенесённых
файлов среди них нет (последний подходящий по названию, #954 про строку
`label_motion_blur_amount`, в `main` уже есть).

Четвёртый баг — собственный. Экраны резервировали под панель фиксированные
96 dp, а панель вместе с полями и жестовой полосой выше почти на всех
телефонах, поэтому последняя строка любого списка оказывалась под ней. Теперь
оболочка измеряет панель и публикует высоту через `LocalBottomBarSpace`.

## Персонализация

Раздел «Взаимодействие» повторяет «Customizations» из Essentials: тема,
цвета из обоев, чёрная тема, вибрация и её сила, свайп между вкладками,
стартовая вкладка, размытие под шторкой, размытие при прокрутке и его сила.
Всё хранится в `AppSettings`, поэтому виджет и фоновая синхронизация видят те
же значения.

Включённый переключатель подкрашивает всю строку, а не только сам тумблер —
`rowSelectedContainer`, смесь цвета строки с `primaryContainer`. Это самая
дешёвая информация на странице: состояние дюжины переключателей читается на
прокрутке, не останавливаясь ни на одной строке. Действие, которое происходит
сразу, а не сохраняет настройку, — «обновить сейчас» — стоит последней строкой
группы залитой кнопкой во всю ширину (`GroupActionItem`), как «Check for
updates» у Essentials.

Не перенесено: выбор иконки приложения и языка (Essentials переключает их
через `activity-alias` и `LocaleManager`, у нас нет ни альтернативных иконок,
ни второй локали в UI), «ripple animation» (эффект рисуется поверх окна
собственным оверлеем Essentials) и «online help media» (у нас нет справки).

## Верхних панелей нет

Ни на одном экране. В Essentials их тоже нет: страница начинается у самого верха
окна, заголовок уезжает вместе с содержимым, а название того, где вы находитесь,
живёт в «таблетке» внизу, которая никуда не девается. Отсюда три следствия, и
все три — причина, по которой это стоило переделать.

Заголовок читается в одном месте. С панелью название экрана было и в ней, и в
выбранной вкладке снизу — одно уезжало при прокрутке, другое нет.

У вложенной страницы появился способ вернуться под большим пальцем. Стрелка
«назад» в верхнем левом углу 6,7-дюймового телефона им не была.

И содержимое может уходить под строку состояния — а без этого верхнее размытие
нечего размывать: непрозрачная панель в этой полосе не оставляла ничего.

Отступ под строку состояния поэтому лежит в `contentPadding` списка, а не в
`padding` вокруг него: обычный отступ остановил бы список ровно перед той
полосой, под которую он должен уходить.

## Размытие сверху появляется по прокрутке

Снизу оно постоянное — под «таблеткой» всегда что-то есть. Сверху нет: в покое
первая строка стоит под строкой состояния, за ней пусто, и размывать пустую
полосу значит просто посадить часы на пятно. Поэтому верхний край набирает силу
за первые ~72 px прокрутки и приходит вместе с содержимым, ради которого он и
нужен.

Шейдер из Essentials считает один радиус на оба края. Здесь их два —
`topRadius` и `bottomRadius` — иначе нижнее размытие исчезало бы и появлялось
вместе с прокруткой заодно с верхним. Ядро, дизеринг и степенная кривая
остались авторскими.

Насколько прокручен экран, знает только сам экран, а оболочка держит сразу
несколько: три страницы пейджера плюс открытую страницу настроек. Поэтому
каждому выдаётся свой `ScrollOffsetHolder`, и оболочка читает тот, что принадлежит
экрану впереди, — иначе страница, стоящая за кадром, перебивала бы видимую.

## Настройки — не вкладка

В «таблетке» три вкладки, а шестерёнка — отдельная кнопка сбоку, как
`fabAction` у Essentials. Настройки были четвёртой вкладкой, и это неправильно:
остальные три — места, в которых живут и между которыми переключаются,
настройки — то, куда заходят, меняют одно и возвращаются. В панели они занимали
четверть ширины у экранов, которым она нужна, и «стартовой вкладкой» можно было
сделать список предпочтений.

Сами настройки разбиты на шесть страниц, в которые проваливаются, — так же, как
`SettingsActivity` открывает `FeatureSettingsActivity`. Две дюжины строк одним
свитком означают прокрутку мимо пяти разделов, за которыми вы не приходили, и
нужная строка никогда не там, где вы её оставили, потому что группы над ней
растут и сжимаются вместе со своими переключателями.

Страница настроек — слой поверх пейджера, а не отдельный граф навигации: вкладки
под ней остаются живыми и сохраняют прокрутку. Перехватывать касания слой не
пытается: Compose проверяет попадание во *все* узлы под пальцем, а не только в
верхний, и проходит поддерево целиком, прежде чем перейти к соседнему. Слой,
который перехватывает достаточно рано, чтобы остановить пейджер, тем же
движением отменяет нажатия на собственных строках — это ровно тот баг, из-за
которого две сборки подряд ни один пункт настроек не открывался, и он закреплён
тестом `OverlayLayerTest`. Поэтому вкладки не накрывают, а убирают из композиции,
когда страница доехала; прокрутку им сохраняет `rememberSaveableStateHolder`.

## Первый запуск — четыре экрана, а не одно поле

Раньше новая установка открывалась полем для кода класса. Это просьба к
незнакомому человеку ввести выданный ему ключ на экране, который ещё ничего о
себе не сказал.

Теперь порядок такой же, как в `WelcomeScreen.kt` у Essentials, и он не
случайный:

1. **Приветствие** — знак приложения, имя и выбор темы. Знак крутится пальцем и
   отпружинивает назад с отдачей: это первое, чего касается пользователь, и оно
   отвечает мгновенно. У Essentials в этом слоте выбор языка; приложение
   выходит на одном языке, поэтому слот отдан теме — второй настройке, эффект
   которой виден на следующем кадре.
2. **Подтверждение** — что приложение показывает, чего оно не делает, и
   единственное, что оно может записать. Отчёты об ошибках выключены по
   умолчанию и выбираются здесь же, рядом с текстом, который объясняет их.
3. **Свойства** — отдача, цвета из обоев, чёрный фон, размытие, учитель в
   строке урока, прогресс в виджете. Не все настройки, а те, о которых можно
   судить, не увидев ни одного урока.
4. **Код класса** — тот же самый `JoinScreen`, а не его копия, с кнопкой назад.

Флаг «введение показано» ставится на входе в четвёртый шаг, а не после удачного
подключения: кто закрыл приложение на поле кода, введение уже прочитал.
Решение, показывать ли введение, защёлкивается при первой композиции — иначе
запись флага подменила бы экран прямо посреди уезжающей анимации. Выход из
класса возвращает на голое поле кода, а не на четыре экрана заново.

## Уведомления

Четыре повода, каждый со своим переключателем, и все выключены при установке.
Приложение, которое начинает жужжать сразу после установки, — это приложение,
которому выключают уведомления системно, и этот канал уже не вернуть.

| Повод | Когда | Канал |
| --- | --- | --- |
| Скоро урок | за 5/10/15/30 минут до звонка | «Уроки», важность High |
| Утренняя сводка | в выбранный час, если сегодня есть уроки | «Расписание на день» |
| Домашка на завтра | вечером, если на следующий учебный день задано | «Расписание на день» |
| Замены и отмены | сразу после синхронизации, которая их нашла | «Изменения» |

Три канала, а не один: тот, кто хочет «через 10 минут алгебра» на экране
блокировки и не хочет напоминания в восемь вечера, должен иметь возможность так
и сказать, а сказать это в Android можно только по каналам.

Время выбирается часами-чипсами, а не часовым пикером. Вопрос — «примерно
когда», ответы, которые имеет смысл давать, — целые часы, и пикер предлагал бы
07:23 так, будто это что-то значит.

Первая строка страницы — доступ. Включить переключатель, когда уведомления
запрещены системно, значит записать настройку, которая молча ничего не делает, —
худший возможный ответ на «почему не работает».

## Смена темы — круг из-под пальца

Эффект, которым известен Telegram, и работает он потому, что отвечает на вопрос,
на который мгновенная подмена ответить не может: *что именно я сейчас изменил?*
Экран, сменивший цвет за один кадр, читается как сбой; та же смена, приехавшая
волной из-под пальца, читается как ответ.

Композиция одна, и после записи настройки она уже новой темы — старую неоткуда
перерисовать. Поэтому анимируется снимок предыдущего кадра, положенный поверх
живого дерева и стираемый растущим кругом. Порядок и есть весь фокус:

1. снять кадр, пока старая тема ещё на экране;
2. записать настройку, чтобы под снимком оказалась новая;
3. открыть в снимке дыру из точки, которой коснулись.

Отсюда и то, что эффект запускается с места вызова, а не наблюдением за потоком
настроек: наблюдатель узнаёт об изменении, когда снимать уже нечего.

Снимок — программная перерисовка `View`, а не `PixelCopy`: `PixelCopy`
асинхронный, а один лишний кадр здесь означает снимок уже новой темы, то есть
круг, открывающийся сам в себя. Платой за это остаются шейдеры размытия —
они аппаратные и в снимок не попадают, так что у него резкие края там, где у
живого дерева мягкие. Под волной, пересекающей экран за полсекунды, этого никто
не видел.

Оверлей — соседний узел, нарисованный после содержимого, и никогда не родитель:
он не должен уметь взять касание, а `Canvas` без pointer input и не умеет. В
этом проекте это не отвлечённая осторожность — полноэкранный слой поверх
настроек однажды съедал все нажатия по всем строкам две сборки подряд.

Круг не проигрывается, если пользователь выключил анимации в системе.

### Волна уходит за экран, а не останавливается у края

Первая версия доводила радиус ровно до самого дальнего угла и вела его кривой,
которая к концу замедляется. Дуга круга пересекает угол медленнее всего, поэтому
последним, что видел человек, была дужка старой темы, сидящая в углу экрана
десятую долю секунды: волна выглядела не закончившейся, а упёршейся. Теперь
радиус на четверть больше нужного, а кривая к концу разгоняется, а не гаснет, —
видимый край сходит с экрана заметно раньше, чем останавливаются часы, и хвост
анимации волна проходит там, где её никто не видит.

### Второе касание не замораживает первое

Часы и снимок теперь живут в одном объекте, который заменяется целиком, и
предыдущая анимация отменяется до того, как опубликован новый. Раньше часы
лежали на состоянии и сбрасывались из корутины, которая их вела: касание,
пришедшее на лету, клало на экран свежий снимок, под которым оставался прогресс
старой анимации — дыра, уже раскрытая на пол-экрана, открывалась на кадр,
снятый мгновение назад. Тот, кто щёлкает темой туда-обратно, чтобы сравнить,
получает по волне на касание, каждую от своего переключателя, и ни одной
застывшей.

## Волна при нажатии на кнопку отладки

Шейдер `liquidRipple` из Essentials: каждый пиксель спрашивает, насколько он
далеко от точки касания, ждёт это расстояние, делённое на скорость волны, и
дальше едет по затухающей синусоиде вдоль луча от источника. Волн две — вторая
чуть позже, медленнее и меньше, — и именно это превращает одно кольцо в нечто
похожее на жидкость.

Запускается с кнопки-жука в оболочке и накладывается на всю оболочку, панель
включительно: волна выходит из кнопки, и панель, оставшаяся неподвижной, выдала
бы, что вышла она откуда-то ещё. Точку даёт сама кнопка — панель плавает, и её
ширина меняется вместе с вкладкой, так что больше её положения не знает никто.

В оригинале исправлены четыре дефекта:

1. **`uResolution` объявлен и не используется.** Это не неряшливость: SkSL
   вырезает униформу, на которую никто не ссылается, а `setFloatUniform` для
   имени, которого в скомпилированной программе уже нет, бросает исключение.
   Здесь она делает то, ради чего, судя по всему, и заводилась, — ограничивает
   выборку, чтобы пиксель, смещённый за край слоя, читал край, а не прозрачную
   пустоту за ним. Пустота рисовалась тёмной каймой, бегущей по периметру
   экрана впереди волны.
2. **Пиксель в источнике даёт NaN.** `normalize` нулевого вектора не определён,
   а пиксель ровно под пальцем — нулевой вектор. Направление теперь деление с
   защитой на одном пикселе, и середина ряби стоит на месте вместо того, чтобы
   стать тем, что драйвер сделает из NaN-координаты.
3. **Блик не считается с альфой.** Оригинал прибавляет плоский белый к
   premultiplied-цвету, а для прозрачного пикселя это даёт цвет ярче
   собственной альфы — невалидное premultiplied-значение, которое видно серой
   дымкой поверх всего полупрозрачного. Умножение на альфу и ограничение ею
   оставляют блик бликом на нарисованном, а не туманом над ненарисованным.
4. **Шейдер работает втрое дольше волны.** В оригинале стоят три секунды при
   затухании 4.5: `exp(−4.5 × 1)` — сотая доля амплитуды, то есть на волне в
   32 dp треть пикселя. Оставшиеся две секунды — полноэкранный runtime-шейдер,
   пересчитывающий невидимое смещение для каждого пикселя шестьдесят раз в
   секунду поверх всего остального приложения; на собственном видео Essentials
   видно, чем за это платят кадры. Здесь стоит время, которое нужно последнему
   фронту: пересечь высокий телефон на 1400 dp в секунду и там затухнуть.

Сам `RuntimeShader` строится в `runCatching`. Конструктор бросает, если чей-то
драйвер не принял исходник, а бросок в композиции уносит приложение — ради
украшения. `null` здесь — оболочка, которая просто не делает ряби.

Ниже Android 13 ряби нет: AGSL там ещё нет.

### Что было сломано в цепочке

Четыре дефекта, найденных разбором уже слитого кода, и все четыре тихие.

**Перепланирование удаляло ближайший алерт.** `reschedule` всегда спрашивала
планировщик о первом событии строго после «сейчас + 90 секунд». Этот сдвиг нужен
ровно одному вызову — тому, который только что опубликовал окно и не должен
опубликовать его дважды. А вызывается перепланирование после каждой синхронизации,
каждой смены настройки, каждого запуска и каждой загрузки, и новый будильник
заменяет стоящий. Синхронизация, случившаяся за минуту до звонка, переписывала
будильник этого звонка следующим, и уведомление не приходило никогда.

**Напоминание о домашке приходило трижды.** Планировщик проходит по всем
закэшированным датам, а «следующий учебный день» пропускает дни без уроков. Домашка
на понедельник была ответом и в пятницу, и в субботу, и в воскресенье — три
одинаковых уведомления под одним id. У утренней сводки строкой выше guard есть, у
домашки не было.

**Неудачное чтение обрывало цепочку.** Цепочка длиной в один будильник: заблокированная
база в момент срабатывания — и позади не остаётся ничего. Функция возвращалась
раньше того единственного места, где взводится следующий. Теперь на этом пути
остаётся повтор.

**Неточный запасной будильник откладывался Doze.** `setWindow` система держит до
следующего окна обслуживания, а сработавший будильник публикует только то, что
попадает в 90 секунд вокруг него, — отложенный на час не публикует ничего.
`setAndAllowWhileIdle` так же неточен, но не откладывается.

Пятое — отпечаток расписания переживал выход из класса, и первая же синхронизация в
новом классе сравнивала его с чужим и объявляла, что расписание изменилось.

## Шторка едет вместе с экраном

Раньше шторка рисовалась один раз, поверх всего, и стояла неподвижно, пока под
ней ехала страница: заход в настройки вдвигал экран под панель, которая уже
показывала его заголовок. Теперь панель принадлежит странице — едут вместе, и у
каждого экрана своя, даже если текст в них повторяется.

В Essentials копировать для этого оказалось нечего. Там каждая страница настроек
это отдельная Activity, и панель «едет вместе» просто потому, что страница —
целое окно: двигает его система, а не код приложения. Никакого Compose-перехода
для настроек там нет вовсе. Эквивалент пришлось собрать самому.

Устройство: один `AnimatedContent` по назначению оболочки (вкладки → корень
настроек → раздел), и каждый слот это `Box { содержимое; шторка }`. Панель
внутри слота, поэтому наследует его сдвиг.

Дисциплина, на которой всё держится: **всё, что показывает шторка, читается из
слота, а не из поднятого состояния.** Во время перехода оба слота в композиции
одновременно, и заголовок, прочитанный снаружи, переключился бы в момент
навигации — уезжающая страница уехала бы с именем приезжающей. По той же причине
исчезла защёлка, хранившая последний раздел: слот несёт свой собственный.

Глубина у назначения объявлена, а не взята из `ordinal`. Порядковый номер это
порядок объявления, и перестановка строк молча развернула бы переход.

`SizeTransform(clip = false)`: по умолчанию анимируется размер слота *и* по нему
же режется содержимое, а страницы настроек сильно разной высоты — более высокую
обрезало бы на всё время перехода. Сдвиг и растворение идут по одной пружине:
при разных кривых приезжающая страница набирает полную непрозрачность, ещё
заметно двигаясь, а уезжающая исчезает, не дойдя до края.

Механизм `tabsCovered` ушёл. Он существовал, потому что слой сверху не может
перехватить касания: `OverlayLayerTest` меряет, почему — слой, потребляющий
достаточно рано, чтобы остановить пейджер, достаточно рано и для того, чтобы
отменить нажатия по собственным строкам. `AnimatedContent` удаляет слот, который
больше не текущий, так что свойство теперь следует из навигации, а не
поддерживается рядом с ней.

Измерение высоты панели теперь на каждой странице своё. Во время перехода панелей
две, и одна общая высота писалась бы дважды за кадр двумя разными панелями.
Верхнее размытие тоже стало собственным: оболочка раньше выбирала экран впереди и
раздавала одно число всем, и уезжающая страница носила его весь переход.

## Разрешения — экран, который существует только когда сломано

Три вещи, каждая из которых отзывается уже после установки и каждая из которых
ломает уведомления по-своему: право показывать уведомления, право на точные
будильники и разрешение работать в фоне. Отозвать любое можно в системных
настройках, через полтора месяца после установки, и приложение узнаёт об этом
только тем, что перестаёт приходить вовремя.

Поэтому на странице уведомлений сверху стоит красная строка «Разрешения — N не
выдано», которая не рисуется вовсе, когда всё на месте. Это не постоянный вход
в раздел, а лампочка неисправности: постоянная строка была бы ещё одним пунктом,
который читают мимо на каждом заходе, и молчала бы на большинстве телефонов, где
всё выдано. Раздел по той же причине спрятан из корневого списка настроек.

На самом экране — красная плашка с кнопкой «проверить снова» и по карточке на
разрешение: название, одна строка о том, что без него не работает, и кнопка
справа. Кнопка остаётся и когда разрешение выдано: страница нужна не только
чтобы чинить — выданное разрешение это то, что приходят и отключить. Меняется
акцент, а не наличие: залитая кнопка, пока что-то не так, обведённая, когда всё
в порядке.

Состояние спрашивается у системы заново, а не хранится: меняется оно снаружи
приложения, и никакого события об этом нет. Пересчёт — на `ON_RESUME` экрана
плюс кнопка вручную. Наблюдатель жизненного цикла, а не `Activity.onResume`, как
в оригинале: здесь это экраны внутри одной активити, и `onResume` активити не
сработал бы при переходе между ними.

Из дефектов оригинала не перенесены четыре. Проверка `POST_NOTIFICATIONS` там
обходит собственный же guard и на Android 12 и ниже навсегда отвечает «не
выдано», потому что этого разрешения там просто нет. Коды запросов не совпадают
с фильтром, который обновляет состояние после ответа, так что обновление
работает случайно — от того, что диалог сам приостанавливает активити. Часть
карточек считает выданность из источников, которых нет в ключах `remember`, и
остаётся устаревшей. И цепочка запасных Intent'ов там повторяет то же действие
без URI: если первое бросило, потому что действие никто не обрабатывает, второе
бросит тоже — и уже не поймано. Здесь список кандидатов заканчивается страницей
приложения, которая есть везде, и каждый пробуется по очереди.

Прямой диалог отключения оптимизации батареи не используется: он требует
объявить `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`, а это разрешение Google Play
проверяет жёстко. Школьный дневник не стоит спора с ревью, поэтому открывается
системный список — на одно нажатие больше.

## Подвал «О приложении»

Последнее на последней странице. Название с версией, описание, знак приложения,
строка об авторе, ссылки таблетками по две в ряд и закрывающая строка.

Знак рисуется так, как его рисует лаунчер: адаптивная иконка — это канва 108 dp,
из которой видно только центральные 72 dp, поэтому прямая отрисовка mipmap дала
бы мелкий знак в поле собственного фона. Передний слой рисуется в обратной
пропорции и обрезается коробкой — тот же кроп, что делает домашний экран.

Из оригинала не перенесено главное: девять кнопок-ссылок там это девять копий
одних и тех же пятнадцати строк, отличающихся иконкой, подписью и адресом, —
поэтому `ActivityNotFoundException` ловит только та одна, что открывает почту, а
на устройстве без браузера остальные восемь роняют приложение. Здесь ссылка это
запись данных, а кнопка одна, так что и перехват, и отдача живут в одном месте.
Версия берётся из `BuildConfig`, а не через `PackageManager` на каждой
рекомпозиции: тот вызов возвращает nullable и печатает буквальное «null», когда
возвращает.

## Размытие теперь и на переходах

Настройка называется «размытие при прокрутке», а самая большая прокрутка в
приложении — свайп между вкладками — под неё не попадала. Теперь попадает, и
вместе с ней два перехода, где экран едет целиком: страница настроек поверх
вкладок и шаг первого запуска. У них нет состояния прокрутки, поэтому размытие
считается по собственной доле перехода: тот же самый анимируемый float, из
которого рисуется сдвиг, помноженный на реальную длину пути. Одно определение
движения на двоих — шейдер и сдвиг не могут разойтись.

## Шторку больше не обрезает при морфинге

Ширину «таблетки» при превращении «три вкладки» → «назад и заголовок» считал
`Modifier.animateContentSize`. Он применяет `clipToBounds` к *анимируемой*
коробке, а таблетка внутри уже разложена на финальную ширину, — поэтому морфинг
проигрывался как стирание двух скруглённых концов, а не как изменение размера.
Отключить этот clip нечем.

Считает теперь `SizeTransform(clip = false)` внутри `AnimatedContent`: он меряет
оба состояния во время перехода и анимирует собственный размер между ними, так
что таблетка в середине морфинга действительно уже, и по дороге ничего не
режется.

## Фон страницы рисует приложение, а не окно

Вкладки рисовали строки и ничего под ними. Между строками было видно **фон
окна** — ресурс Android, который следует ночному режиму системы и настройку
внутри приложения видеть не может.

Пока система и приложение совпадали, это выглядело намеренным. «Светлая» тема на
телефоне в тёмном режиме давала белые строки и чёрный текст на чёрной странице, а
«чёрная тема» как будто не работала вовсе: единственная поверхность в
приложении, которая красила себя сама, — слой настроек, и ровно там обе
настройки и «работали».

Теперь `LessonsApp` красит `surfaceContainer` под всем сразу. Тем же изменением
исправлены системные полосы: `enableEdgeToEdge()` сам по себе спрашивает ночной
режим *системы*, а смысл настройки темы в том, что приложение имеет право с
системой не соглашаться — светлое приложение под тёмной системой рисовало белые
часы и батарею на белой странице. Активность перевызывает `enableEdgeToEdge` с
`detectDarkMode`, который смотрит на `ThemeMode.resolvesToDark()`.

## Вложенных пейджеров больше нет

Внутри вкладки «Неделя» лежал второй пейджер, из семи дней: внутренний забирал
жест первым, внешний получал только то, что осталось на краю. Теперь день
выбирается нажатием, период листается стрелками, а вкладка называется
«Календарь» и показывает три масштаба: неделю, месяц и день по часам.
Выключается в настройках — «Свайп между вкладками».

Часовая лента — единственный из трёх, где блоки стоят по времени, а не идут
подряд. Сорокаминутное окно между уроками в списке выглядит как две строки
подряд; на линейке оно выглядит как окно. События получают колонку рядом с
уроками, потому что наложение события на урок — это информация, а список
наложение показать не может.

## Сколько виджет говорит на каждом размере

Лестница размеров — одиннадцать ступеней, и каждая объявляет свою порцию
флагами прямо на `WidgetSizeClass`, а не отдельной копией разметки. Тело
собирает из них четыре блока в том порядке, в каком о них спрашивают:

1. **сейчас** — что идёт, сколько это длится, сколько осталось и насколько
   пройдено;
2. **дальше** — следующий урок и неделя;
3. **сегодня** — всё оставшееся и что было задано на сегодня;
4. **впереди** — домашка на следующий учебный день и как этот день выглядит.

| Ступень | Точка | Что добавляет |
| --- | --- | --- |
| TINY | 110×40 | состояние и сколько осталось |
| WIDE | 250×60 | + предмет на той же строке |
| SMALL | 110×110 | + прогресс |
| SMALL_TALL | 110×190 | + «дальше» и счётчик домашки |
| NARROW | 110×300 | + остаток дня узкими строками |
| MEDIUM | 250×110 | + колонка «Дальше» |
| MEDIUM_TALL | 250×180 | + остаток дня |
| LARGE | 250×250 | + полоса недели |
| XLARGE | 320×320 | + домашка |
| TALL | 320×400 | + следующий учебный день |
| HUGE | 320×560 | + больше всего этого |

Блок «впереди» появляется и раньше своей ступени, если на сегодня осталась
строка или ни одной: именно в пять вечера высокий виджет и оказывался наполовину
пустым, и именно тогда единственный полезный вопрос — про завтра.

Две вещи в отсчёте были сломаны. `AndroidRemoteViews` без модификатора размера
раскладывается как «заполнить», поэтому в строке рядом с взвешенной подписью он
забирал всю ширину, и слово «ПЕРЕМЕНА» меряли в ноль — виджет показывал голое
«05:57» и ни разу не говорил, к чему это число. А `Chronometer` умеет рисовать
только цифры, и в обратном счёте до часа это `MM:SS`, что над предметом читается
как «без трёх шесть». Теперь он оборачивает содержимое и носит рядом слово — «до
звонка», «до начала».

## Чего виджет не может

Три вещи из запроса упираются не в лень, а в `RemoteViews`, через который Glance
рисует всё на домашнем экране.

**Анимации нет.** У виджета нет кадрового цикла: система забирает у процесса
дерево `RemoteViews` и рисует его в лаунчере. Единственное движущееся, что
разрешено, — `Chronometer` и `ProgressBar`, оба со своей встроенной логикой.
Поэтому качающийся звонок живёт в приложении, на карточке состояния во время
перемены: там это Compose, два поворота вокруг точки подвеса (`rotationZ` для
размаха и `rotationY` для ухода от зрителя) при короткой `cameraDistance`, так
что дальняя сторона обода действительно сужается — это настоящая перспектива, а
не подрисованный блик.

**Горизонтальной прокрутки нет.** Из прокручиваемых контейнеров `RemoteViews`
даёт `ListView`, `GridView` и `StackView`, и все три вертикальные. Неделя
поэтому вписана, а не прокручена: семь фиксированных колонок с буквой дня,
числом и точками по числу уроков. Каждая — ссылка в приложение на этот день,
что и было второй половиной смысла прокрутки.

**Живое число может быть только одно.** Виджет — это снимок: процесс просыпается
по будильнику, рисует и засыпает. Будильник раз в секунду система не доставит,
поэтому отсчёт до звонка отдан `Chronometer` в режиме обратного счёта — он
живёт в процессе лаунчера и перерисовывает себя сам. У Glance такого элемента
нет, он приходит через `AndroidRemoteViews`.

**И десять детей на контейнер.** Готовые макеты Glance держат варианты на
ноль–десять детей, а транслятор берёт первые десять и молча выбрасывает
остальное. Отсюда `CHILD_LIMIT` и обёртки-колонки вокруг каждого списка.

## Известно и пока не исправлено

Найдено разбором уже слитого кода, проверено, но не входит в эту порцию правок.

**Круг смены темы открывается на старой теме.** Порядок «снять кадр, записать
настройку, открыть дыру» описан в самом файле, но не выполняется: `change()` — это
`viewModelScope.launch { … }` поверх DataStore, то есть возвращается на первом же
приостанове. Несколько кадров под снимком остаётся прежняя тема, и новая
проступает уже внутри раскрытого круга — ровно та мгновенная подмена, которую
эффект должен был спрятать. Чинится ожиданием фактической смены темы перед стартом
анимации.

**Иконки системных полос перекрашиваются раньше снимка.** Тинт полос меняется в тот
же кадр, что и тема, а снимок поверх окна ещё старый, — и пока волна не дойдёт до
верха экрана, светлые иконки рисуются поверх светлого снимка. Дольше всего это
видно при смене из группы «Оформление», которая лежит низко.

**Снимок экрана делается на главном потоке.** Программная перерисовка всего дерева
в битмап на каждое переключение — заметный затык перед началом волны.

**Морфинг шторки не проигрывается.** Оболочка передаёт `items = emptyList()` при
открытии настроек, поэтому уходящий ряд вкладок перерисовывается пустым и иконки
исчезают за один кадр вместо превращения в кнопку «назад».

**Широкий виджет всё ещё прячет домашку.** 250×300 попадает в ступень LARGE, у
которой `showsHomework` выключен, а 110×300 — в NARROW, где включён. Просто
включить флаг нельзя: бюджет высоты LARGE уже израсходован полосой недели и шестью
строками расписания. Нужна либо новая ступень для «широкого и высокого», либо
перераспределение бюджета внутри LARGE.

**Виджет утверждает то, чего не знает.** При `DayState.NoData` он проходит обычным
путём и печатает «Уроков больше нет» и «ДЗ на сегодня · Ничего не задано» под
заголовком «НЕТ РАСПИСАНИЯ» — оба вывода сделаны из отсутствующих данных. Рядом:
число предметов считается в одном месте по записям, в другом по уникальным
предметам, так что виджет может сам себе противоречить.

## Ограничения

**Шрифт в виджете.** Виджет остаётся на системном шрифте. Glance передаёт
`fontFamily` в `RemoteViews` как имя системного семейства, а не как ресурс
шрифта, поэтому подключить `google_sans_flex.ttf` тем же способом, что и в
приложении, нельзя.

**Лицензия шрифта.** `google_sans_flex.ttf` взят из Essentials, где он лежит
под MIT вместе с остальным репозиторием. Сам по себе Google Sans Flex —
фирменная гарнитура Google, а не шрифт со свободной лицензией, поэтому
распространять её в опубликованном APK юридически небезопасно. Замена — одна
строка: уберите файл из `:core:designsystem/res/font/` и замените
`GoogleSansFlex` в `theme/Type.kt` на `FontFamily.Default` или на любую
гарнитуру под OFL.

## Жирный — это состояние

У Essentials нет отдельной системы акцентов для текста: выбранный сегмент
пикера набран жирным, его соседи — обычным, и этого достаточно. Здесь то же
правило распространено на всё, у чего есть «выбранное», «текущее» или «в
фокусе»: жирный вес получает только тот элемент, который сейчас включён, а
соседи остаются в весе шкалы. Вес работает там, где цвет не работает — на
заливке `primary`, на подкрашенной строке, в оттенках серого, — и один тяжёлый
ярлык в ряду из семи находится раньше, чем прочитан хоть один. Обратная
сторона правила: среди равных ничто не жирное «для красоты», иначе состояние
перестаёт что-либо значить. Заголовки страниц и кнопки первого запуска в этот
ряд не входят — у них нет соседей, с которыми они спорят.

Правило живёт в одной строке — `TextStyle.emphasised(active)` в
`theme/Emphasis.kt` — и через неё проходят: выбранный сегмент `SegmentedPicker`;
подпись открытой вкладки в шторке; выбранный день и сегодняшний в полосе недели
и в сетке месяца; предмет урока, который идёт сейчас, в `LessonRow` (соседи
остаются в `Medium`, чтобы не выпадать из остальных строк); чипы «сейчас» и
«сегодня» — `PillChip` с `selected = true` при своих же цветах; подписи полей
кода класса и адреса сервера, пока поле в фокусе. Выбранный час в настройках
уведомлений получает то же через `PillChip`, ничего не зная о правиле.

## Обновления приходят с GitHub

Приложение не в магазине, и единственное место, где появляется новая версия, —
релиз на GitHub с подписанным APK, который собирает workflow по тегу `v*`.
Проверка обновлений спрашивает у API список релизов и сравнивает с тем, что
установлено. Сравнение — отдельный тип `AppVersion` в `:core:model`, по
правилам семантических версий: `0.10.0` новее `0.9.0` как число, а не как
строка; `1.0.0-rc.1` старше `1.0.0`; `rc.10` после `rc.2`. Черновики не
предлагаются никогда, пре-релизы — только по отдельному переключателю.
Отдельно учтён суффикс `-debug`, который отладочная сборка добавляет к
`versionName`: semver читает его как пре-релиз, и без обрезки отладочная
установка текущей версии вечно предлагала бы себе саму себя.

Проверка на запуске идёт не чаще раза в сутки — не из экономии, а потому что
неавторизованное окно GitHub, шестьдесят запросов в час, считается на адрес, и
класс телефонов за одним роутером его делит. Неудачная проверка тоже
засчитывается: отказ тратит окно так же, как ответ. Ручная проверка на странице
«Обновления» этого ограничения не знает.

Ответ показывается шторкой — той же для «доступно обновление» и для «всё
актуально», как в Essentials: во втором случае с заметками к установленному
выпуску, потому что «что у меня стоит» — тоже ответ. Заметки — Markdown из
тела релиза, для которого нужен не рендерер, а разбор заголовков, списков,
`**жирного**`, `` `кода` `` и ссылок; всё остальное — абзац. Кнопка «Обновить»
открывает APK в браузере, дальше работает системный установщик; приложение
ничего не скачивает и ничего не ставит само. «Позже» запоминает тег, и
автоматическая проверка больше не поднимает шторку на этот выпуск — ручная
поднимет.

Два ответа дают две волны. «Обновить» пускает рябь от кнопки наружу, как любое
нажатие в приложении; «Позже» — ту же рябь в обратную сторону, кольцо
сходится к кнопке. Для этого в шейдер добавлен `uReverse`: он меняет и порядок
прихода фронта (дальние углы первыми), и направление смещения пикселей. Шторка
— отдельное окно, поэтому центр кнопки измеряется в координатах экрана, а
оболочка переводит его в свои.

## Вход через GitHub нужен ради одного

Чтобы сообщить об ошибке, не выходя из приложения. Вход — device flow: код на
экране, страница GitHub в браузере, приложение опрашивает токен с интервалом,
который назвал GitHub, плюс секунда запаса. Это единственный OAuth-грант,
который работает без сервера и без секрета — и секрета здесь нет намеренно:
APK не умеет хранить тайну, а утекший client secret позволил бы отзывать чужие
токены. Цена — токен нельзя отозвать из приложения; «выйти» его только
забывает, а из списка авторизованных приложений на GitHub его убирают там.

Client id — не константа в коде, а свойство сборки `lessons.github.clientId`
(или `LESSONS_GITHUB_CLIENT_ID`): OAuth App регистрирует тот, кто собирает.
Без него строка входа не показывается вовсе — строка, которая открывает
шторку со словами «не настроено», была бы строкой о сборке, а не о
пользователе. Токен и логин пишутся одной транзакцией после того, как профиль
получен, так что на диске не бывает токена без имени рядом; получение токена
не отменяется закрытием шторки — иначе пользователь набирал бы второй код за
первый токен, который уже висит в его списке.

Отчёт об ошибке уходит тремя путями, и вход нужен только для первого: issue от
имени пользователя через API; страница «новый issue» в браузере с уже
заполненными заголовком и телом; письмо на адрес из `lessons.contactEmail` —
тоже свойство сборки, потому что адрес в публичном репозитории — адрес в
каждом спам-списке. Тело во всех трёх случаях одно: описание и таблица об
устройстве в том же составе, что у Essentials.

## Два выключателя для двух украшений

Волна и круговая смена темы — оба на весь экран, и оба теперь можно выключить
по отдельности в «Эффектах». Волна — шейдер на каждом кадре полторы секунды
поверх всего приложения, единственное украшение здесь, которое дешёвый телефон
чувствует; она делит доступность с размытием. Круговая смена — растровый
снимок, идёт на чём угодно и не выключается сама, но её просят выключить те,
кому от такого укачивает. Обе теперь запускаются вместе при смене темы: новая
тема расходится кругом, а впереди неё — кольцо ряби из-под того же пальца.
Выключатели независимы: волна без круга и круг без волны — оба честные
состояния.
