<?xml version="1.0" encoding="utf-8"?>
<!--
  Rounded surface for the static layouts; Glance uses cornerRadius() instead.

  24dp, which is WidgetSurfaceCorner. It was 20, and the four dp showed: this is
  the initialLayout the launcher inflates while the process wakes — which the
  loading layout's own comment says can be most of a minute on a cold start — so
  on API 31+ the corner visibly stepped as the first real render landed.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <solid android:color="@color/widget_preview_surface" />
</shape>
