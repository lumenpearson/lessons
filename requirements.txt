# Vercel installs from the repository root. The server's own dependency set
# lives in server/pyproject.toml; this file mirrors it because Vercel's Python
# builder does not read pyproject from a subdirectory.
#
# asyncpg only: the serverless database is Postgres, and aiosqlite would be
# dead weight in the function bundle.
fastapi>=0.141.1
aiogram>=3.31.0
sqlalchemy[asyncio]>=2.0.52
asyncpg>=0.31.0
pydantic>=2.8
pydantic-settings>=2.15.0
# Level with server/pyproject.toml, ceiling included: every diary call passes
# per-request cookies, which httpx 0.28 warns it is removing, and a release
# that removed them would send every diary call without a session rather than
# fail. What to check before raising it is written out in pyproject.toml.
httpx>=0.28.1,<0.29
cryptography>=50.0.1
